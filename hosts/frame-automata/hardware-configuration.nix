# PLACEHOLDER. A throw, not a stub: wrong disk UUIDs build an unbootable
# system, so this must fail at eval rather than produce something plausible.
throw ''
  hosts/frame-automata/hardware-configuration.nix is still the placeholder.
  On the desktop, run:
    nixos-generate-config --show-hardware-config > hosts/frame-automata/hardware-configuration.nix
''
